# 🧠 Rooty / Alpha – API-Based GPT Mortgage Assistant

This version of Rooty uses **your OpenAI API key** to power a secure, white-labeled experience — no login required for users.

## 🔧 Setup

1. Clone or unzip this repo
2. Copy `.env.local.example` → `.env.local`
3. Add your OpenAI API key:
   ```
   OPENAI_API_KEY=sk-...
   ```
4. Install dependencies:
   ```
   npm install
   ```
5. Start development:
   ```
   npm run dev
   ```

## 🚀 Deploy to Vercel
- Go to [vercel.com/import](https://vercel.com/import)
- Select your GitHub repo
- Add `OPENAI_API_KEY` as an environment variable

You're live! ✅
